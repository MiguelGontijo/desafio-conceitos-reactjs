import React from 'react';

function Header({ title }) {
    return (
        <header>
            <h1>{title}</h1>
            <br/>
            <br/>
        </header>
    );
}

export default Header;
